<div class="home d-flex justify-content-center align-self-center flex-column">
  <div>
    <h1 class="job">JobTimer</h1>
    <% if @user == current_user %>
    <%= link_to 'Start now !', projects_path, class: 'start' %>
    <% else %>
    <%= link_to 'Start now !', new_user_session_path, class: 'start' %>
    <% end %>
  </div>
</div>
